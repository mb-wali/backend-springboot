# spring boot 

**develop on ubuntu**
```bash
# install openjdk
sudo apt update
sudo apt install openjdk-17-jdk

# verify
java -version

# install maven
sudo apt install maven

# verify
mvn -version

# run
mvn spring-boot:run

```
